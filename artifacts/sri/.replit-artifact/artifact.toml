kind = "web"
previewPath = "/"
title = "SRI – Release Go Live Control"
version = "1.0.0"
id = "artifacts/sri"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24472

[services.development]
run = "pnpm --filter @workspace/sri run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sri", "run", "build" ]
publicDir = "artifacts/sri/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24472"
BASE_PATH = "/"
